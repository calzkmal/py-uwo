""" 

Author: Calzy Akmal Indyramdhani
Student ID: 251397118
Date Created: September 14, 2023

"""

width = input("Input the width of the rectangle: ") # Take the user input and store it in width variable
length = input("Input the length of the rectangle: ") # Take the user input and store it in length variable
rec_area = float(width) * float(length) # Calculate the rectangle area by multiplying and changing width and length variables to float, also changing the 
print(rec_area)

base = input("Input the base of the triangle: ") #  Take the user input and store it in width variable
height = input("Input the height of the triangle: ")
tri_area = (float(width) * float(length)) / 2
print(tri_area)

area = input("Input the radius of the circle: ")
cir_area = 3.14 * (float(area) * float(area))
print(cir_area)

